# Music Genre Statistical Learning Project

This repository is structured to match the course deliverables while keeping the analysis in a single `R` script.

## Notes

- The `R` code is intentionally kept in a single file because the assignment requires one `.R` submission.
- The report source is separate from the analysis code so the PDF can stay clean and contain no `R` commands.
